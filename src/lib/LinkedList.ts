export class Node {
    next: Node | null = null;

    constructor(public data: number){};
}

export class LinkedList {
    // constructor(public data: ){};

    // get length() {

    // };

    compare() {

    };

    swap(){

    };
}
